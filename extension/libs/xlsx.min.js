// Note: For production, download the actual XLSX library from https://cdn.jsdelivr.net/npm/xlsx/
// This is a placeholder - you'll need to add the real library
// In popup.html and content.js, the extension will fallback to CSV if XLSX is not available

// Placeholder for XLSX library
if (typeof XLSX === 'undefined') {
  window.XLSX = { writeFile: function(){} }; // Graceful fallback
}
